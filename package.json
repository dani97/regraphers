{
  "name": "graphql-project-builder",
  "version": "1.0.0",
  "description": "project building tool for projects consuming graphql endppoints",
  "main": "index.js",
  "author": "Christopher Daniel <Christopher Daniel>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^5.15.1",
    "prettier": "^1.16.4"
  },
  "scripts": {
    "build:frontend": "yarn workspace graphql-hackers run build",
    "build:backend": "yarn workspace graphql-server run start",
    "watch:frontend": "yarn workspace graphql-hackers run dev; cd - >/dev/null",
    "watch:backend": "yarn workspace graphql-server run dev:start; cd - >/dev/null"
  },
  "engines": {
    "node": ">=10.7.0",
    "yarn": ">=1.12.0"
  }
}
