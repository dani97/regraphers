{
  "name": "graphql-project-builder",
  "version": "1.0.0",
  "description": "project building tool for projects consuming graphql endppoints",
  "main": "index.js",
  "author": "Christopher Daniel <Christopher Daniel>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^5.15.1",
    "prettier": "^1.16.4"
  },
  "scripts": {
    "build": "yarn workspaces graphql-server run build",
    "build:dev": "yarn workspaces run build:dev",
    "bundlesize": "npx bundlesize",
    "clean:all": "yarn workspaces run clean && rimraf ./node_modules",
    "clean:dist": "yarn workspaces run clean",    
    "watch:frontend": "yarn workspace graphql-hackers run start; cd - >/dev/null",
    "watch:backend": "yarn workspace graphql-server run start; cd - >/dev/null"
  },
  "engines": {
    "node": ">=10.7.0",
    "yarn": ">=1.12.0"
  }
}
