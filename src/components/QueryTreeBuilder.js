import React from 'react';
import { getTreeData } from "../services/project";

const QueryTreeBuilder = () => {
    return(
        <div>
            <h3>This is tree builder</h3>
        </div>
    );
}

export default QueryTreeBuilder;